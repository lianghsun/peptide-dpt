# Core ML
torch>=2.4.0                    # Blackwell (B200) needs torch >= 2.4
transformers>=4.44.0
trl>=0.10.0
accelerate>=0.33.0
deepspeed>=0.15.0

# Chemistry
selfies>=2.1.1
rdkit>=2023.9.1

# Data
datasets>=2.14.0
huggingface_hub>=0.24.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
chembl-webresource-client>=0.10.9
biopython>=1.81
tqdm>=4.66.0

# Training infra
pyyaml>=6.0
wandb>=0.16.0
scipy>=1.11.0

# Optional speedups
flash-attn>=2.6.0               # install separately: pip install flash-attn --no-build-isolation
ninja                           # needed to build flash-attn
